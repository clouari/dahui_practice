import 'package:flutter/material.dart';

class FakeData extends StatelessWidget {
  const FakeData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('dd'),
      ),
      body: Container(),
    );
  }
}
